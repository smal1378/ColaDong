[project]
name = "coladong"
version = "0.1.0"
description = "Cola Dong — a shared lending ledger for a friend group"
requires-python = ">=3.13"
dependencies = [
    "django~=6.1",
    "gunicorn~=23.0",
]
